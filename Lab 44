import numpy as np



def softmax_naive(scores):
    exp_scores = np.exp(scores)
    return exp_scores / np.sum(exp_scores)
scores = np.array([1.0, 2.0, 3.0])
print("Probs:", softmax_naive(scores), "Sum:", softmax_naive(scores).sum())
